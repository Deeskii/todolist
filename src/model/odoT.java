package model;
import model.Todo.*;
public class odoT {

    public Todo getCount(int getCount)
    {
        Todo one = new Todo();
        one.setTask("");
        one.setId(0);
        return one;
    }
}
